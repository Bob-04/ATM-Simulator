<UserControl Width="380" Height="280" x:Class="KMA.MOOP.ATM.UI.Views.ReplenishCashMenuView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KMA.MOOP.ATM.UI.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1.3*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Replenish money" FontSize="25" Height="45" HorizontalAlignment="Center" Margin="0,40,0,0"/>
        <Label Grid.Row="1" Content="Enter sum of money you want to replenish" Height="35" Width="380" FontSize="19" Margin="0,30,0,0" />
        <TextBox Grid.Row="2" Name="NumberTextBox" IsEnabled="False" FontSize="26" Height="50" TextAlignment="Center" Text="{Binding ReplenishSum, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" PreviewTextInput="NumberValidationTextBox" Margin="110,7"/>
    </Grid>
</UserControl>
